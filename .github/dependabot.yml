# Keeps the pinned GitHub Actions (full SHAs) current: Dependabot proposes each new release as a PR.
version: 2
updates:
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
